# reNavigator
An simple .apk android service that runs in background and listens for notifications containing #nav then extracts location from that notification to forward it to selected navigation app with "go" button. almost 0 distraction for driver.
